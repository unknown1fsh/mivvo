{
  "buildCommand": "cd frontend && npm install && cd ../api && npm install && npx prisma generate && cd ../frontend && npm run build",
  "outputDirectory": "frontend/.next",
  "installCommand": "npm install --prefix frontend && npm install --prefix api",
  "framework": "nextjs",
  "functions": {
    "api/index.ts": {
      "runtime": "@vercel/node@3.0.0"
    }
  },
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api/index"
    }
  ]
}

