[phases.setup]
nixPkgs = ["nodejs_18", "npm"]

[phases.install]
cmds = [
  "npm ci",
  "cd backend && npm ci",
  "cd frontend && npm ci"
]

[phases.build]
cmds = [
  "npm run build"
]

[start]
cmd = "npm run start:full"

[variables]
NODE_ENV = "production"
PORT = "3000"
